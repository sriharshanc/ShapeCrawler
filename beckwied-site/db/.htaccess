# Block direct web access to the schema.sql file. It is imported once via
# phpMyAdmin/the Strato database panel and should never be reachable over HTTP.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
